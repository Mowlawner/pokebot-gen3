[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra

markers =
    unit: fast tests that do not require an emulator ROM
    emulator: tests that boot libmgba and require emulator state
    e2e: long-running end-to-end campaign or bot tests
